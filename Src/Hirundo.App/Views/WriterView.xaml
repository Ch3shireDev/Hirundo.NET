<UserControl x:Class="Hirundo.App.Views.WriterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Hirundo.App.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" d:Background="AliceBlue"
             d:DataContext="{d:DesignInstance viewModels:WriterViewModel}"
             >
    <StackPanel Margin="10">
        <WrapPanel Margin="10">
            <TextBlock TextWrapping="WrapWithOverflow">W tym panelu wybierasz sposób zapisu wyników.</TextBlock>
        </WrapPanel>
        <WrapPanel Margin="10">
            <ProgressBar MinWidth="100" />
            <Button>Przetwórz dane</Button>
        </WrapPanel>
        <WrapPanel Margin="10">
            <Label MinWidth="100" />
            <Button>Wybierz ścieżkę pliku wynikowego</Button>
        </WrapPanel>
        <WrapPanel Margin="10">
            <Button Command="{Binding SaveDataCommand}">Zapisz dane</Button>
        </WrapPanel>
    </StackPanel>
</UserControl>